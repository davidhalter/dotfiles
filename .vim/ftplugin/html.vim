" For html we don't want a fixed textwidth, because it naturally gets very
" large with a lot of indentations.
set textwidth=0
set wrap


let g:surround_116 = "{% trans \"\r\" %}"

" A shortcut for mapping the translation of django in visual mode.
xmap t St
